# Credentials
BOT-TOKEN: "MTI5NjYwNDQ1NDQ1MTQxNzEzMQ.Gv1ncr.16-MujVsAg6kvt7qXvXu7ZWrvgmmj41BqjjR30" # Accquire from Discord Developer Panel

# Database
DEFAULT-DATABASE: "data/default.json" # Default database for TinyDB to use
BACKUP-DATABASE: "data/backup.json" # Backup database to recover from if something happens. Saves every X minutes unless forcibly shut down by Host
BACKUP-MINUTES: 10 # Timer to backup the database and copy data from DEFAULT-DATABASE to BACKUP-DATABASE
ENABLE-BACKUP: true # To disable the feature entirely. Just change the "true" value to "false". (NOT RECOMMENDED)


# Logging
LOGGING: true # Enable logging from OpenRewards
LOG-FILE: "app.log" # Set what file logs should default to, Factory setting is app.log

# Github Details 
RELEASE: "DEV1"

# Permissions
STAFF-ROLES: [] ## Staff roles, These will be the roles able to access point/rewards accounts. Add, Remove **with limits**.
ADMIN-ROLES: [] ## Admin roles, These will be the roles similar to staff roles. But **without limits**, These roles will also be able to Delete, Give and Manage user items.
OWNER-IDS: [] ## User ID's of the owner of the bot. These user ID's have FULL control of the bot. Additional permissions such as completly deleting user profiles, and more adminstrative features.

# Profile Configuration
ICON: "" ## The markdown of a certain emoji to put infront of all refrences of points
DELETE-ON-LEAVE: false ## Boolean, Delete the users profile if they leave the server, Setting the value to true will allow this to happen.


## Staff Role Permissions
MAX_ADD: 10000 # Ranges from 0 to X, The amount a staff member is allowed to give points to a user.
MAX_SUB: 10000 # Ranges from 0 to X, The amount a staff member is allowed to remove points from a user.
